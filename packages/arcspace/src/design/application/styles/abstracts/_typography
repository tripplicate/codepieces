:root {
  --app-font-family-primary: "Open Sans", var(--app-font-family-fallback);
  --app-font-family-fallback:
    system-ui,
    -apple-system,
    blinkmacsystemfont,
    "Segoe UI",
    roboto,
    oxygen,
    ubuntu,
    cantarell,
    "Open Sans",
    "Helvetica Neue",
    sans-serif;
}
